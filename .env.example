# This file serves as a template for environment variables across the project

# Frontend Environment Variables (piponne-app/.env)
REACT_APP_API_URL=http://localhost:3001/api

# Backend Environment Variables (piponne-back/.env)
PORT=3001
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/piponne
CORS_ORIGIN=http://localhost:3000
